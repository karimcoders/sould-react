# GITHUB REPOSITORY SETUP
Enter `FTP_PASSWORD`, `FTP_SERVER`, `FTP_USERNAME` in github repository -> settings -> secrets and variables -> actions to deploy on hostinger using FTP.
Github actions is already setup to auto deploy on every push on the main branch.